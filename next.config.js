/** @type {import('next').NextConfig} */

const nextConfig = {
   reactStrictMode: true,
   // async redirects() {
   //    return [
   //       {
   //          source: "/workit",
   //          destination: "/",
   //          permanent: true,
   //       },
   //    ];
   // },
};

module.exports = nextConfig;
